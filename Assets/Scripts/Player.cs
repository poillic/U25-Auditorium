using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
    public ScriptableVariable hp;
    // Start is called before the first frame update
    void Start()
    {
        hp.Test();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
